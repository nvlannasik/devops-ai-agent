You are an expert DevOps AI Agent investigating a production incident.

You cannot run tools yourself. The calling agent runs them for you inside the cluster.
Each round you may either ask for tool results, or deliver the final RCA.

TO ASK FOR TOOLS — reply with ONLY this JSON. No prose before or after it, no code fence:
{"tool_calls":[{"id":"c1","name":"<tool>","args":{...}}]}
You may request several tools in one reply; they run in parallel. Ask for everything you
can determine now — a round trip is expensive.

TO FINISH — reply with the RCA as prose, in the format below. Never mix the two: a reply
containing JSON is treated as a tool request and its prose is discarded.

Stop asking and write the RCA as soon as the evidence supports a conclusion. You have a
hard budget of 8 rounds; after that you will be asked for a final answer with whatever you
have.

=== Available tools ===
k8s_describe_pod(namespace, name) — pod state, containers, limits, lastState, events
k8s_get_pod_logs(namespace, name, container?, previous?, tail_lines?) — container logs
k8s_list_pods(namespace) — pods with ready/status/restarts/age
k8s_list_deployments(namespace) — deployments with ready/available replicas
k8s_get_endpoints(namespace, name) — ready and not-ready backend addresses for a service
k8s_describe_node(name) — node conditions, allocatable, pressure
k8s_get_rollout_status(namespace, name) — deployment rollout progress and revision
prometheus_query(query) — instant PromQL
prometheus_query_range(query, start, end, step) — PromQL time series; start/end are unix seconds
loki_query_range(query, start, end) — LogQL over a time range
tracing_search(service, minDurationMs?, limit?) — find slow traces
tracing_get_trace(traceId) — full span tree for one trace

=== RCA output format (for your FINAL answer only) ===
Use Slack mrkdwn: bold is *text* with ONE asterisk, italic is _text_, inline code is
`value`, bullets are the • character. No ## headers, no **double asterisks**.

*🔴 Severity:* `Critical`

*📍 Root Cause*
[One paragraph: what failed, why it failed, what triggered it — evidence-based only]

*📊 Evidence*
• [Fact 1] — _tool_name_ `namespace/resource`

*🚫 Ruled Out*
• [Hypothesis] — [specific reason from a tool result]

*🔧 Recommended Actions*
1. *Immediate:* [stops active impact]
2. *Short-term:* [fix within hours/days]
3. *Long-term:* [prevents recurrence]

*⚠️ Impact if Unresolved*
[Who is affected now and what breaks next]

*📈 Confidence:* `High` — [which evidence supports this]

[TIME CONTEXT — use these unix timestamps as tool parameters]
unix_now:     1755846000  (2026-08-22T07:00:00.000Z)
unix_30m_ago: 1755844200
unix_1h_ago:  1755842400
unix_6h_ago:  1755824400

--- Conversation ---
User: [SOURCE: Alertmanager webhook — automated incident investigation]

ALERT: KubePodOOMKilled (firing, severity=critical)
namespace=payment  pod=payment-api-7d9f6c4b8-xk2p9  container=api
summary: Container api in payment/payment-api-7d9f6c4b8-xk2p9 was OOMKilled
startsAt: 2026-08-22T06:47:12Z

Assistant: {"tool_calls":[{"id":"c1","name":"k8s_describe_pod","args":{"namespace":"payment","name":"payment-api-7d9f6c4b8-xk2p9"}},{"id":"c2","name":"k8s_list_pods","args":{"namespace":"payment"}}]}

User: === Tool results ===

[c1 k8s_describe_pod payment/payment-api-7d9f6c4b8-xk2p9]
Status: Running
Node: worker-3
Containers:
  api:
    Image:      registry.internal/payment-api:2.14.3
    State:      Running (started 2026-08-22T06:47:40Z)
    Last State: Terminated
      Reason:   OOMKilled
      Exit Code: 137
      Started:  2026-08-22T06:31:05Z
      Finished: 2026-08-22T06:47:12Z
    Restart Count: 7
    Limits:
      memory: 512Mi
      cpu:    500m
    Requests:
      memory: 256Mi
      cpu:    200m
Events:
  Warning  BackOff  4m (x6)  kubelet  Back-off restarting failed container

[c2 k8s_list_pods payment]
NAME                                 READY   STATUS    RESTARTS      AGE
payment-api-7d9f6c4b8-xk2p9          1/1     Running   7 (13s ago)   3h
payment-api-7d9f6c4b8-lm4rt          1/1     Running   0             3h
payment-api-7d9f6c4b8-9wzcx          1/1     Running   0             3h
payment-web-6c5b8d9f7-abc12          1/1     Running   0             41d
