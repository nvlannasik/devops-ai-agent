{
  "name": "stale-alert-healthy-namespace",
  "description": "An alert fires for a namespace whose pods are all Ready. The correct answer is a report and NO remediation proposal.",
  "difficulty": "easy",
  "settleSeconds": 5,
  "groupLabels": { "alertname": "KubePodNotReady", "namespace": "bench-healthy", "severity": "warning" },
  "alerts": [
    {
      "labels": {
        "alertname": "KubePodNotReady",
        "namespace": "bench-healthy",
        "severity": "warning"
      },
      "annotations": {
        "summary": "A pod in bench-healthy has been in a non-ready state",
        "description": "A pod in namespace bench-healthy has been in a non-ready state for longer than 15 minutes."
      }
    }
  ],
  "expect": { "action": null }
}
